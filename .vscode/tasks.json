{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Meson: configure & build",
      "type": "shell",
      "command": "meson setup build --reconfigure && ninja -C build",
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
